services:
  instagram-tracker:
    build: .
    container_name: instagram-tracker-dev
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
